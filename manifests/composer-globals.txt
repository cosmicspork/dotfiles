# Composer global packages — manually required (transitive deps not listed).
# Installed by bootstrap.sh on every profile; missing packages are required at
# their latest version. Manually: composer global require $(grep -v '^#' < manifests/composer-globals.txt | xargs)
laravel/installer
laravel/cloud-cli
laravel/lsp
cpx/cpx
